title = "Fernando Family Astrology secret scanning configuration"

[extend]
useDefault = true

[allowlist]
description = "Browser-storage identifier, not a credential"
regexTarget = "match"
regexes = [
  '''ff_private_vault_(rotation|rekey)_journal_v1''',
]
