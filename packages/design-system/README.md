# design-system

Reserved for shared UI primitives (buttons, cards, activity-color tokens) if
they need to be shared beyond `apps/web/components/`. Not used yet — the
initial Pancha Pakshi UI lives directly in `apps/web/components/`.
