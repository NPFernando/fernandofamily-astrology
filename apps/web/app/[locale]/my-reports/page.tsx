import Link from "next/link";
import { SavedReportsWorkspace } from "@/components/reports/SavedReportsWorkspace";
import { getDictionary } from "@/lib/i18n";
import { resolveLocale } from "@/lib/page-metadata";
export default async function MyReportsPage({ params }: { params: Promise<{ locale: string }> }) { const locale = await resolveLocale(params); const dict = getDictionary(locale); return <div className="flex w-full flex-col gap-6"><header><h1 className="text-2xl font-bold">{dict.ui.myReports}</h1><p className="mt-1 opacity-75">{dict.ui.deviceOnly}</p><div className="mt-3 flex flex-wrap gap-2"><Link href={`/${locale}/profiles`} className="inline-flex rounded-lg border px-3 py-1.5 text-sm font-semibold">{dict.ui.manageProfiles}</Link><Link href={`/${locale}/compare-reports`} className="inline-flex rounded-lg border px-3 py-1.5 text-sm font-semibold">{dict.ui.compareReports}</Link><Link href={`/${locale}/insights`} className="inline-flex rounded-lg border px-3 py-1.5 text-sm font-semibold">{dict.ui.insights}</Link></div></header><SavedReportsWorkspace /></div>; }
