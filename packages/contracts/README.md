# contracts

Reserved for shared TypeScript types derived from the API's OpenAPI schema,
once codegen is wired up. Not used yet — `apps/web/lib/api-client.ts`
currently hand-writes its request/response types to mirror
`apps/api/app/modules/pancha_pakshi/{requests,models}.py` directly.
